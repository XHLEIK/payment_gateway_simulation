// PostCSS processor configurations.
// Standard configuration using tailwindcss's new postcss plugin (v4).
const config = {
  plugins: {
    // Process styling using TailwindCSS
    "@tailwindcss/postcss": {},
  },
};

export default config;
